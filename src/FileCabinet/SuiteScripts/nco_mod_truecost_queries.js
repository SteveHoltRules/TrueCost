/**
 * @NApiVersion 2.1
 */
define(['N/query', 'N/record', 'N/search'],
    /**
 * @param{query} query
 * @param{record} record
 * @param{search} search
 */
    (query, record, search) => {

        const foo = () => {

        }

        const bar = () => {

        }

        return {foo, bar}

    });
